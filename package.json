{
  "type": "module",
  "scripts": {
    "dev": "nodemon --watch src --ignore src/**/*.test.ts --exec tsx src/api/index.ts",
    "test": "vitest run --reporter verbose"
  },
  "dependencies": {
    "express": "^5.0.1",
    "node-cron": "^4.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.0.6",
    "nodemon": "^3.1.10",
    "vitest": "^3.2.4"
  }
}
